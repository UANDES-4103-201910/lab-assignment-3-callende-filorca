<%= form_with(model: admin, local: true) do |form| %>
  <% if admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin.errors.count, "error") %> prohibited this admin from being saved:</h2>

      <ul>
      <% admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :admin_profile_id %>
    <%= form.text_field :admin_profile_id %>
  </div>

  <div class="field">
    <%= form.label :super_admin_id %>
    <%= form.text_field :super_admin_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
